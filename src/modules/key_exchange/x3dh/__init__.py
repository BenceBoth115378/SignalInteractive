"""X3DH protocol package.

The X3DH package contains the educational implementation of the classical
identity + signed prekey + one-time prekey exchange. Its logic, module, view,
and visualization files work together to expose the registration flow, bundle
request flow, secret derivation, and initial message verification steps in the
interactive UI.
"""
